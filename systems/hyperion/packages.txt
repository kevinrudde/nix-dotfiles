# One package name per line.
# Official repo packages and AUR packages can both be installed through paru.

# Hyprland / UWSM session
hyprland
uwsm

# Login manager
greetd
greetd-tuigreet

# Secrets + auth agents 
gnome-keyring
hyprpolkitagent

# Fonts
ttf-jetbrains-mono
ttf-jetbrains-mono-nerd

# Core Wayland desktop tools
ghostty
foot
vicinae-bin
fuzzel
wireplumber
wl-clipboard
grim
slurp
brightnessctl
playerctl

# Hypr ecosystem
hypridle
hyprlock
quickshell
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
nautilus

# Intel Arc graphics (mesa stack)
mesa
vulkan-intel
intel-media-driver

# Qt Wayland support
qt5-wayland
qt6-wayland
qt6ct
kvantum

# Multimedia codecs
ffmpeg
gst-plugins-base
gst-plugins-good
gst-plugins-bad
gst-plugins-ugly
gst-libav

logiops

# The lookahead limiter that ends the speaker tuning graph in
# home/features/speaker-tuning is an LV2 plugin. Without it the filter-chain
# fails to instantiate and the tuning sink silently never appears.
lsp-plugins-lv2

intel-lpmd
thermald

# Dell publishes XPS 16 BIOS and Thunderbolt firmware through LVFS, and the
# Panther Lake platform firmware is still moving quickly -- worth being able to
# check for updates without booting Windows.
fwupd

# Apps
1password
1password-cli
zen-browser
docker
tailscale
helium-browser-bin
google-chrome
slack-desktop
teams-for-linux
obsidian

# Intel IPU7 MIPI webcam (OV08X40 behind the Panther Lake IPU).
# Dependencies of the vendored intel-ipu7-camera PKGBUILD, which
# pkgbuild-sync.sh builds after this list is installed. linux-cachyos-headers
# is listed explicitly because DKMS needs a header tree for every installed
# kernel, and the PKGBUILD depends on it by name.
dkms
linux-cachyos-headers
linux-cachyos-lts-headers
v4l2loopback-dkms
v4l2-relayd
gstreamer
jsoncpp
libdrm

# DKMS toolchain. Both CachyOS kernels on this host are clang + ThinLTO
# builds, so out-of-tree modules must be compiled with LLVM=1 -- gcc rejects
# the clang-only flags their Kbuild emits. Needed on every kernel upgrade,
# not just the first install.
clang
llvm
lld
