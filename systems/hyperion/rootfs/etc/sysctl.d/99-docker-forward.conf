# Docker normally sets this itself when it manages iptables, but daemon.json
# disables that (iptables: false, delegating to firewalld) so dockerd refuses
# to start with "IPv4 forwarding is disabled" unless we set it ourselves.
net.ipv4.ip_forward = 1
