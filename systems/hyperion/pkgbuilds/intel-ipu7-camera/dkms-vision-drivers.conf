# Local delta vs upstream: LLVM=1. The CachyOS kernel is built with
# clang + ThinLTO (CONFIG_CC_IS_CLANG=y, CONFIG_LTO_CLANG_THIN=y), so its
# Kbuild passes clang-only flags like -fsplit-lto-unit and -mllvm. Building
# these modules with the default gcc fails on those flags; LLVM=1 points
# Kbuild at the same toolchain the kernel itself was built with.

PACKAGE_NAME="vision-drivers"
PACKAGE_VERSION="@PKGVER@"
AUTOINSTALL="yes"

BUILT_MODULE_NAME[0]="intel_cvs"
BUILT_MODULE_LOCATION[0]=""
DEST_MODULE_LOCATION[0]="/updates"

MAKE[0]="make LLVM=1 KERNEL_SRC=${kernel_source_dir}"
CLEAN="make LLVM=1 KERNEL_SRC=${kernel_source_dir} clean"
